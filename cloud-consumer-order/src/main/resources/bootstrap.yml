server:
  port: 80

spring:
  application:
    name: cloud-consumer-payment
  cloud:
    config:
      label: master
      name: order
      profile: dev
      uri: http://localhost:3344
  rabbitmq:
    host: 192.168.220.131

#暴露监控端点
management:
  server:
    port: 80
  endpoints:
    web:
      exposure:
        include: "*"

